animations {
    enabled = true

    bezier = default, 0.16, 1, 0.3, 1
    bezier = overshot, 0.34, 1.56, 0.64, 1

    animation = windows, 1, 5, overshot, popin 92%
    animation = windowsOut, 1, 4, default, popin 92%
    animation = windowsMove, 1, 5, default

    animation = fade, 1, 3, default

    animation = layers, 1, 3, default, slide
    animation = layersOut, 1, 3, default, slide

    animation = border, 1, 5, default
    animation = borderangle, 1, 8, default

    animation = workspaces, 1, 5, default, slide
    animation = specialWorkspace, 1, 5, default, slidevert
}
